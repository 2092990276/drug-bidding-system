<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.drug.bidding.dao.UserDAO">
    <select id="findByUsername" parameterType="String" resultType="User">
        SELECT * FROM user WHERE username = #{username}
    </select>
    <insert id="insert" parameterType="User">
        INSERT INTO user(username, password, type, company_name, contact_name, phone, address)
        VALUES(#{username}, #{password}, #{type}, #{companyName}, #{contactName}, #{phone}, #{address})
    </insert>
    <select id="findById" parameterType="Integer" resultType="User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <update id="update" parameterType="User">
        UPDATE user SET password = #{password}, company_name = #{companyName}, 
        contact_name = #{contactName}, phone = #{phone}, address = #{address}
        WHERE id = #{id}
    </update>
    <delete id="delete" parameterType="Integer">
        DELETE FROM user WHERE id = #{id}
    </delete>
    <select id="findAll" resultType="User">
        SELECT * FROM user
    </select>
</mapper>